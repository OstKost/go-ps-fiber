package components

templ Title2(title string) {
	<h2 class={ title2Class() }>{ title }</h2>
}

css title2Class() {
	width: 100%;
	margin: 0;
	padding: 0;
	font-weight: 700;
	font-size: 50px;
	text-align: center;
	color: { templ.SafeCSSProperty("var(--color-black)") };
}
