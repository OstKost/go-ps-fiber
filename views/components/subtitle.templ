package components

templ Subtitle(text string) {
	<h2 class={ subtitleClass() }>{ text }</h2>
}

css subtitleClass() {
	width: 100%;
	height: 25px;
	font-size: 18px;
	text-align: center;
	color: #BABABA;
}
