package components

templ Title3(title string) {
	<h3 class={ title3Class() }>{ title }</h3>
}

css title3Class() {
	width: 100%;
	margin: 0;
	padding: 0;
	font-weight: 400;
	font-size: 16px;
	text-align: center;
	color: { templ.SafeCSSProperty("var(--color-black)") };
}
