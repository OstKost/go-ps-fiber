package layout


css headerClass() {
	display: flex;
	flex-direction: column;
	align-items: center;
	
	height: 601px;

	background: linear-gradient(0deg, rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), url(./public/header_bg.png);
}

templ Header() {
	<header class={headerClass()}>
		{ children... }
	</header>
}
